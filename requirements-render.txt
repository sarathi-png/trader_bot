# Render-optimized requirements — strips heavy ML/deps for 512 MB Free tier.
# Keep only what's needed for mock mode + basic signal generation.
# Live mode (Playwright/Chromium) would need separate env handling.

python-dotenv>=1.0.0
pyquotex @ git+https://github.com/cleitonleonel/pyquotex.git
python-telegram-bot>=20.0
aiosqlite>=0.19.0
APScheduler>=3.10.0
websockets>=12.0
cloudscraper>=1.2.71
httpx>=0.28.0
# Removed: pandas, pandas-ta, numpy, playwright — too heavy for 512 MB Free.
# Re-add if you upgrade to a paid Render plan with more RAM.